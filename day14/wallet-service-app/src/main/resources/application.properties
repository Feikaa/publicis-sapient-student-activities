server.port=9091

# if eureka server runs in a different location, set prefer ip = true and set url to defaultZone
eureka.instance.prefer-ip-address=true
eureka.client.service-url.defaultZone=http://localhost:8070/eureka/